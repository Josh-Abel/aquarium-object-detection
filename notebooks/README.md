# Notebooks

The runnable project code lives in `src/aquarium_detection/`.

Use `demo_inference.ipynb` for a lightweight checkpoint loading and validation-image inference walkthrough after adding the dataset and model weights locally.
